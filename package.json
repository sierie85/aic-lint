{
  "name": "aic-lint",
  "version": "0.1.0",
  "description": "Local, deterministic AI context optimizer & config linter: measures your always-on per-session token budget, ranks files by weight and lost-in-the-middle position, and lints CLAUDE.md/AGENTS.md/Cursor configs.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "github:sierie85/aic-lint"
  },
  "type": "module",
  "bin": {
    "aic-lint": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "audit": "tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json && node -e \"require('fs').chmodSync('dist/index.js', 0o755)\"",
    "prepack": "npm run build",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
